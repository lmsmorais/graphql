type Book {
    id: ID!
    title: String!
    author: String!
}

type Query {
    bookById(id: ID!): Book
    hello: String
}

type Mutation {
    addBook(id: ID!, title: String!, author: String!): Book
}